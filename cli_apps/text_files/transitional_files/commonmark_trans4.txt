descriptioncommonmark.py is a pure Python port of "’s"", aMarkdown parser and renderer for the"" specification, using only nativemodules. Once both this project and the CommonMark specification arestable we will release the first "" version and attempt to keep upto date with changes in "."commonmark.py is tested against the CommonMark spec with Python versions2.7, 3.4, 3.5, 3.6, and 3.7." 0.9.1  descriptioncommonmark.py is a pure Python port of "’s"", aMarkdown parser and renderer for the"" specification, using only nativemodules. Once both this project and the CommonMark specification arestable we will release the first "" version and attempt to keep upto date with changes in "."commonmark.py is tested against the CommonMark spec with Python versions2.7, 3.4, 3.5, 3.6, and 3.7." 0.9.1  descriptioncommonmark.py is a pure Python port of "’s"", aMarkdown parser and renderer for the"" specification, using only nativemodules. Once both this project and the CommonMark specification arestable we will release the first "" version and attempt to keep upto date with changes in "."commonmark.py is tested against the CommonMark spec with Python versions2.7, 3.4, 3.5, 3.6, and 3.7." 0.9.1  descriptioncommonmark.py is a pure Python port of "’s"", aMarkdown parser and renderer for the"" specification, using only nativemodules. Once both this project and the CommonMark specification arestable we will release the first "" version and attempt to keep upto date with changes in "."commonmark.py is tested against the CommonMark spec with Python versions2.7, 3.4, 3.5, 3.6, and 3.7." 0.9.1  descriptioncommonmark.py is a pure Python port of "’s"", aMarkdown parser and renderer for the"" specification, using only nativemodules. Once both this project and the CommonMark specification arestable we will release the first "" version and attempt to keep upto date with changes in "."commonmark.py is tested against the CommonMark spec with Python versions2.7, 3.4, 3.5, 3.6, and 3.7." 0.9.1  